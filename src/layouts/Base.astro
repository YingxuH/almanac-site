---
import '../styles/global.css';

interface Props {
  title: string;
  description?: string;
}
const { title, description = "The modern almanac for an uncertain future. AI-powered daily forecasts about humanity's trajectory." } = Astro.props;
---
<!doctype html>
<html lang="en" class="light">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="description" content={description} />
  <meta property="og:title" content={title} />
  <meta property="og:description" content={description} />
  <meta property="og:type" content="website" />
  <meta property="og:url" content="https://yingxuh.github.io/almanac-site/" />
  <meta name="twitter:card" content="summary_large_image" />
  <meta name="twitter:title" content={title} />
  <meta name="twitter:description" content={description} />
  <title>{title}</title>
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=JetBrains+Mono:wght@400;500&family=Noto+Sans+SC:wght@400;500;600;700&display=swap" rel="stylesheet" />
  <script is:inline>
    (function() {
      var t = localStorage.getItem('almanac-theme');
      if (t === 'dark') document.documentElement.classList.replace('light', 'dark');
      else document.documentElement.classList.add('light');
      // Apply language BEFORE first paint
      var lang = localStorage.getItem('almanac-lang') || 'en';
      document.documentElement.setAttribute('data-lang', lang);
    })();
  </script>
</head>
<body class="bg-almanac-bg text-almanac-text font-sans antialiased min-h-screen transition-colors duration-300">

  <!-- Nav -->
  <nav class="fixed top-0 w-full z-50 border-b border-almanac-border bg-almanac-bg/80 backdrop-blur-xl">
    <div class="max-w-6xl mx-auto px-4 sm:px-6 h-14 flex items-center justify-between">
      <a href="/almanac-site/" class="flex items-center gap-1.5 text-lg font-semibold tracking-tight">
        <span class="text-almanac-accent">A</span>lmanac
      </a>

      <!-- Desktop nav -->
      <div class="hidden sm:flex items-center gap-5 text-sm text-almanac-muted">
        <a href="/almanac-site/forecast" class="hover:text-almanac-text transition" data-en="Forecast" data-zh="今日预测">Forecast</a>
        <a href="/almanac-site/predictions" class="hover:text-almanac-text transition" data-en="Predictions" data-zh="预测追踪">Predictions</a>
        <a href="/almanac-site/dashboard" class="hover:text-almanac-text transition" data-en="Dashboard" data-zh="数据看板">Dashboard</a>
        <a href="/almanac-site/archive" class="hover:text-almanac-text transition" data-en="Archive" data-zh="历史">Archive</a>
        <a href="/almanac-site/about" class="hover:text-almanac-text transition" data-en="About" data-zh="关于">About</a>
        <a href="/almanac-site/validation" class="hover:text-almanac-text transition" data-en="Validation" data-zh="准确率">Validation</a>
        <button id="lang-toggle" aria-label="Toggle language"
          class="w-9 h-9 flex items-center justify-center rounded-lg border border-almanac-border hover:border-almanac-muted bg-almanac-surface transition-all cursor-pointer text-[10px] font-bold tracking-wider">
          <span id="lang-label" class="text-almanac-accent">EN</span>
        </button>
        <button id="theme-toggle" aria-label="Toggle theme"
          class="w-9 h-9 flex items-center justify-center rounded-lg border border-almanac-border hover:border-almanac-muted bg-almanac-surface transition-all cursor-pointer">
          <svg id="icon-sun" class="w-4 h-4 text-almanac-amber" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
            <circle cx="12" cy="12" r="5"/><path d="M12 1v2M12 21v2M4.22 4.22l1.42 1.42M18.36 18.36l1.42 1.42M1 12h2M21 12h2M4.22 19.78l1.42-1.42M18.36 5.64l1.42-1.42"/>
          </svg>
          <svg id="icon-moon" class="w-4 h-4 text-almanac-accent hidden" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
            <path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"/>
          </svg>
        </button>
      </div>

      <!-- Mobile hamburger -->
      <button id="menu-toggle" class="sm:hidden w-9 h-9 flex items-center justify-center rounded-lg border border-almanac-border bg-almanac-surface" aria-label="Menu">
        <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
          <path id="menu-open-icon" d="M4 6h16M4 12h16M4 18h16"/>
          <path id="menu-close-icon" class="hidden" d="M6 18L18 6M6 6l12 12"/>
        </svg>
      </button>
    </div>

    <!-- Mobile menu -->
    <div id="mobile-menu" class="hidden sm:hidden border-t border-almanac-border bg-almanac-bg/95 backdrop-blur-xl">
      <div class="px-4 py-4 space-y-3">
        <a href="/almanac-site/forecast" class="block text-sm text-almanac-muted hover:text-almanac-text transition py-1" data-en="Forecast" data-zh="今日预测">Forecast</a>
        <a href="/almanac-site/predictions" class="block text-sm text-almanac-muted hover:text-almanac-text transition py-1" data-en="Predictions" data-zh="预测追踪">Predictions</a>
        <a href="/almanac-site/dashboard" class="block text-sm text-almanac-muted hover:text-almanac-text transition py-1" data-en="Dashboard" data-zh="数据看板">Dashboard</a>
        <a href="/almanac-site/archive" class="block text-sm text-almanac-muted hover:text-almanac-text transition py-1" data-en="Archive" data-zh="历史">Archive</a>
        <a href="/almanac-site/about" class="block text-sm text-almanac-muted hover:text-almanac-text transition py-1" data-en="About" data-zh="关于">About</a>
        <a href="/almanac-site/validation" class="block text-sm text-almanac-muted hover:text-almanac-text transition py-1" data-en="Validation" data-zh="准确率">Validation</a>
        <a href="https://github.com/YingxuH/almanac" class="block text-sm text-almanac-muted hover:text-almanac-text transition py-1">GitHub</a>
        <div class="pt-2 border-t border-almanac-border flex items-center gap-2">
          <span class="text-xs text-almanac-muted" data-en="Theme" data-zh="外观">Theme</span>
          <button id="theme-toggle-mobile" class="w-8 h-8 flex items-center justify-center rounded-lg border border-almanac-border bg-almanac-surface">
            <svg class="w-4 h-4 text-almanac-amber theme-sun" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
              <circle cx="12" cy="12" r="5"/><path d="M12 1v2M12 21v2M4.22 4.22l1.42 1.42M18.36 18.36l1.42 1.42M1 12h2M21 12h2M4.22 19.78l1.42-1.42M18.36 5.64l1.42-1.42"/>
            </svg>
            <svg class="w-4 h-4 text-almanac-accent hidden theme-moon" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
              <path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"/>
            </svg>
          </button>
        </div>
      </div>
    </div>
  </nav>

  <main class="pt-14">
    <slot />
  </main>

  <footer class="border-t border-almanac-border mt-20 py-12">
    <div class="max-w-6xl mx-auto px-6 text-center text-sm text-almanac-muted space-y-3">
      <p data-en="Almanac — The modern almanac for an uncertain future." data-zh="Almanac — 软件工程的现代年鉴。">Almanac — The modern almanac for an uncertain future.</p>
      <p class="text-xs max-w-lg mx-auto" data-en="I built this because I kept reading 'AI will replace all developers' hot takes and wanted actual probabilities, not anxiety. So I built a pipeline that reads 17+ sources every morning and updates 55 predictions using Bayesian math. One person, one pipeline, $2.45/day. If I stop running it, the code and data are open source." data-zh="受够了「AI要干掉程序员」的贩卖焦虑，想用概率说话。一个人、一条 pipeline、每天 $2.45。如果哪天我不做了，代码和数据全部开源。">I built this because I kept reading "AI will replace all developers" hot takes and wanted actual probabilities, not anxiety. So I built a pipeline that reads 17+ sources every morning and updates 55 predictions using Bayesian math. One person, one pipeline, $2.45/day. If I stop running it, the code and data are open source.</p>
      <p class="text-xs" data-en="&quot;We're cartographers, not prophets.&quot;" data-zh="「我们是制图者，不是预言家。」">"We're cartographers, not prophets."</p>
    </div>
  </footer>

  <script is:inline>
    (function() {
      // Language system — apply IMMEDIATELY and on every page load event
      function applyLang(l) {
        document.querySelectorAll('[data-en][data-zh]').forEach(function(el) {
          el.textContent = el.getAttribute('data-' + l) || el.textContent;
        });
        document.querySelectorAll('[data-en-html][data-zh-html]').forEach(function(el) {
          el.innerHTML = el.getAttribute('data-' + l + '-html') || el.innerHTML;
        });
        document.querySelectorAll('[data-en-placeholder][data-zh-placeholder]').forEach(function(el) {
          el.placeholder = el.getAttribute('data-' + l + '-placeholder') || el.placeholder;
        });
        document.querySelectorAll('#lang-label').forEach(function(el) {
          el.textContent = l === 'zh' ? '中' : 'EN';
        });
      }

      // Apply on initial script execution (before first paint)
      var lang = localStorage.getItem('almanac-lang') || 'en';
      document.documentElement.setAttribute('data-lang', lang);
      applyLang(lang);

      // Re-apply after DOM is fully ready (catches late-rendered elements)
      document.addEventListener('DOMContentLoaded', function() {
        var l = localStorage.getItem('almanac-lang') || 'en';
        applyLang(l);
      });

      // Re-apply on Astro client-side navigation
      document.addEventListener('astro:page-load', function() {
        var l = localStorage.getItem('almanac-lang') || 'en';
        applyLang(l);
      });

      // Toggle handler
      document.querySelectorAll('#lang-toggle').forEach(function(btn) {
        btn.addEventListener('click', function() {
          var current = document.documentElement.getAttribute('data-lang') || 'en';
          var next = current === 'en' ? 'zh' : 'en';
          document.documentElement.setAttribute('data-lang', next);
          localStorage.setItem('almanac-lang', next);
          applyLang(next);
        });
      });

      // Theme toggle
      function updateIcons() {
        var isLight = document.documentElement.classList.contains('light');
        document.querySelectorAll('#icon-sun, .theme-sun').forEach(el => el.classList.toggle('hidden', isLight));
        document.querySelectorAll('#icon-moon, .theme-moon').forEach(el => el.classList.toggle('hidden', !isLight));
      }
      updateIcons();
      document.querySelectorAll('#theme-toggle, #theme-toggle-mobile').forEach(btn => {
        btn.addEventListener('click', function() {
          var html = document.documentElement;
          if (html.classList.contains('light')) {
            html.classList.replace('light', 'dark');
            localStorage.setItem('almanac-theme', 'dark');
          } else {
            html.classList.replace('dark', 'light');
            localStorage.setItem('almanac-theme', 'light');
          }
          updateIcons();
        });
      });

      // Mobile menu
      var menuBtn = document.getElementById('menu-toggle');
      var menu = document.getElementById('mobile-menu');
      var openIcon = document.getElementById('menu-open-icon');
      var closeIcon = document.getElementById('menu-close-icon');
      if (menuBtn) {
        menuBtn.addEventListener('click', function() {
          menu.classList.toggle('hidden');
          openIcon.classList.toggle('hidden');
          closeIcon.classList.toggle('hidden');
        });
      }
    })();
  </script>
</body>
</html>
